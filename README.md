# ADHDed-Planner-Maker

## 🎯 목적
1. 자발적 학습 및 계획 수립이 서투른 나를 위한 플래너 기획
2. adhd인들 누구나 쉽게 원하는 요소를 포함 / 제거할 수 있는 플래너 파일 생성

## ✅ 포함된 기능
1. 월간 / 주간 / 일간 플래너 동적 생성
2. 양력 기준 고정된 공휴일들 및 대체 공휴일 판별 함수 구축
3. 입력된 방향 키 값에 따라 전후 페이지 이동

## 🔜 앞으로 추가될 수 있는 기능(미정)
1. 음력 기준 공휴일들 판별 함수
2. 시작 요일 변경 기능
3. 출력할 기간 및 포함할 플래너 양식 수정 기능

## 🔁 패치 노트

### 2026-09-03 13:50
1. 효율성에 따른 기존 플래너들의 섹션 및 코드 재구성
2. 기존 폴더 속 오탈자 수정

### 2026-09-03 15:30
1. 기간별(월간/주간/일간) 분류 대신 기능·데이터별 분류로 renual 폴더 신설, main_edit_screen 기준 main.html 및 styles/scripts/data 구조 작성
2. daily_planner / weekly_planner / monthly_planner를 renual 구조로 이식하며 중복된 휴일 판별·날짜 계산 로직과 공휴일/테마/명언 데이터를 공용 모듈로 통합
3. 미리보기(info-preview) 축소 시 정중앙 정렬되도록 스케일링 로직 수정 (주간 화면만 좌측 상단 기준 유지)
4. 이식 과정에서 발견한 대체공휴일 제외 로직 버그, 주간 하단 박스가 화면 밖으로 넘치던 문제 수정

### 2026-09-05 21:40
1. 하단 additional 버튼에 출력 설정 모달 추가 (포함할 항목 월/주/일 다중 토글, 출력 범위 시작일·종료일 선택, 표지 시안 선택) 및 확인 시 선택한 항목만 사이드바 잠금 해제되도록 연동
2. `toDayName()`이 날짜(getDate)를 요일(getDay) 대신 잘못 사용하던 요일 계산 버그 수정
3. 월간/주간/일간 화면이 날짜 상태 하나를 공유해서, 한 화면에서 방향키로 이동한 뒤 다른 화면으로 넘어가면 엉뚱한 날짜가 표시되던 문제 수정 — `appState`에 화면별 독립 상태(monthlyState/weeklyState/dailyState)를 분리해 화면 간 이동이 서로 간섭하지 않도록 개선
4. 주간 플래너 헤더·할 일 목록 텍스트에 테마 색상 적용